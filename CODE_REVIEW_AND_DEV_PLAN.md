# nctool 代码审查与后续开发计划

审查日期：2026-09-05  
审查范围：当前 `master` 工作区的未提交 Web UI 增量、既有 CLI/Core 调用链、前端 API 契约、路线图与测试覆盖。

## 结论摘要

项目的 CLI 主链路（模板解析、参数校验、G-code 生成、机床预设和 golden 回归）基础较好。当前工作区正在把 `nctool ui` 从占位命令升级为本地 HTTP 服务，方向正确：默认监听回环地址、路由可纯函数测试、请求体有 1 MiB 上限，并且 `render` 复用了 Core 的真实生成管线。

但当前增量不宜直接以“可用 Web UI”提交或发布。存在一个必须先修复的高危本地文件读取风险；同时，服务所返回的页面仍固定为演示模式，未使用真实后端数据，且前后端校验报告字段不一致。现状应定义为“后端骨架及部分交互 API 已实现，尚未完成安全收口、前端接线和端到端验收”。

## 审查发现

### P0-1：HTTP 接口可读取模板目录以外的本地文本文件

- 严重性：CRITICAL
- 置信度：10/10
- 指纹：`WEBUI-LFI-001`
- 位置：`cli/src/server.rs:123-186, 460-485`；`cli/src/context.rs:124-135`

`GET /api/templates/{name}` 和 `POST /api/inspect` 将 HTTP 请求里的模板名传入 `Ctx::find_template_file()`。该函数支持绝对路径；当配置了模板目录时，也会直接执行 `template_dir.join(name_or_path)`，允许 `..` 逃逸目录。模板详情接口会将读取的内容原样作为 `source` 返回。

这使得本地或局域网访问者能够通过路径或 URL 编码路径读取进程可读的 UTF-8 文本文件。显式使用 `--host 0.0.0.0` 时，影响会扩大为未认证远程读取。

**修复要求**：HTTP API 仅接受注册表模板 ID；不要复用 CLI 的“支持任意本地路径”行为。若必须支持目录模板，解码后拒绝绝对路径、Windows 前缀和 `..`，对模板根目录与候选项分别规范化并验证 `candidate.starts_with(root)`，同时覆盖符号链接逃逸。

### P0-2：服务页面仍固定使用演示数据，不是真实后端 UI

- 严重性：CRITICAL
- 置信度：10/10
- 指纹：`WEBUI-CONTRACT-001`
- 位置：`ui/index.html:1384-1404, 1536-1546, 2062-2066`；`cli/src/server.rs:26-27, 547-553`

服务端用 `include_str!` 原样提供 `ui/index.html`，而页面将 `API.mode` 固定为 `demo`，启动时直接使用静态 `TEMPLATES` 与 `MACHINE_PRESETS`。页面不会请求 `/api/templates`、`/api/templates/{name}`、`/api/machines` 或 `/api/render`。

**影响**：目录模板、自定义机床和 Core 的实际渲染结果均未进入浏览器；下载的 G-code 可能与 CLI 不一致。

**修复要求**：`nctool ui` 场景默认走同源 server 模式；启动时异步加载模板和机床，选择模板时请求详情，并用后端返回内容驱动参数表、源码和结果。离线演示模式可保留，但必须显著提示“结果不代表真实生成”。

### P0-3：校验报告 DTO 前后端字段与等级不一致

- 严重性：CRITICAL
- 置信度：10/10
- 指纹：`VALIDATION-DTO-001`
- 位置：`cli/src/server.rs:257-276`；`ui/index.html:1764-1782`

后端发出 `{ level: "warning", message: "..." }`，前端读取 `i.level === "warn"` 与 `i.msg`。即使切换为服务模式，警告数量、颜色和文案都会错误。

**修复要求**：以强类型 `Serialize` / `Deserialize` DTO 固化契约，统一为 `level: error|warning|info`、`param`、`message`；前端停止使用 `msg` / `warn`。为 `/api/validate` 与 `/api/render` 建立 JSON schema 或契约断言。

### P1-1：显式非回环监听只告警、无认证

- 严重性：WARNING
- 置信度：10/10
- 指纹：`REMOTE-EXPOSURE-001`
- 位置：`cli/src/commands/ui.rs:15-26`；`cli/src/server.rs:385-408, 514-517`

默认值 `127.0.0.1` 是合理的；但 `--host 0.0.0.0` 仅打印警告，服务仍向局域网暴露模板源码、机床配置和接口。P0-1 修复前该组合尤其危险。

**建议**：默认只允许回环。远程监听应要求额外的 `--allow-remote` 开关；若确有远程使用需求，再引入一次性 token、鉴权、最小化响应字段和部署说明。

### P1-2：HTTP 测试只验证 `/health`，覆盖不足

- 严重性：WARNING
- 置信度：10/10
- 指纹：`UI-E2E-HEALTH-ONLY`
- 位置：`cli/tests/cli.rs:631-674`；`cli/src/server.rs:656-755`

当前真实进程测试只验证端口可响应 `/health`。缺少对首页、模板列表/详情、`validate`、`render`、非法 JSON、未知机床、413、路径守卫、目录模板和自定义机床的验证，因此无法发现 P0-1 至 P0-3。

**建议**：补三层测试：纯路由单元测试、真实 HTTP 集成测试、服务模式前端/API 适配测试。所有常驻服务测试必须保留随机端口、启动超时和强制回收。

### P1-3：生成选项解析会静默退回默认值

- 严重性：WARNING
- 置信度：8/10
- 指纹：`OPTIONS-SILENT-FALLBACK-001`
- 位置：`cli/src/server.rs:303-320`

错误类型的布尔值、负数/越界行号或未知 `format` 不会返回 400，而是静默替换为默认值。这会产生“请求成功但选项未生效”的难排查行为。

**建议**：改为强类型请求对象和 `Result<GenerationOptions, ApiError>`；对已知字段严格校验，非法值返回 400。

### P2：可维护性与运行时加固

1. `server.rs` 与 CLI 命令层分别实现了参数、校验、生成与报告映射，后续容易漂移。应提取共享的应用服务层，让 CLI/HTTP 仅负责传输适配。
2. 每个 API 请求会重新构建注册表；模板目录较大时将重复读盘。启动时缓存注册表，或实施受控刷新机制。
3. 1 MiB 请求体限制是正向控制，但单线程同步读取未设置读取超时、渲染/输出限制和并发保护。远程模式必须补这些限制。
4. `--host ::1` 会拼成非标准的 `http://::1:8787` / `::1:8787`；应使用 `SocketAddr` 和 IPv6 方括号格式，并先绑定成功再执行 `--open`。
5. `docs/PROJECT_STATUS.md` 与实际增量不一致：`/api/validate`、`/api/render` 已在 `server.rs` 路由中实现，不再是 404；真正阻塞项是安全、契约、前端接线和验收。

## 推荐开发计划

### 里程碑 S0：阻断安全风险并冻结 UI API（P0，先于提交）

1. 拆分 CLI 与 HTTP 的模板解析入口：Web API 仅允许注册表模板 ID。
2. 实现目录模板路径守卫和负向测试：绝对路径、`..`、百分号编码、符号链接、模板根目录外文件。
3. 明确远程监听策略：保守方案为仅允许回环；如保留远程模式，要求显式确认并增加 token。
4. 定义并实现强类型 API DTO：模板、机床、校验报告、渲染请求与响应。
5. 用真实 HTTP 请求补齐 `/`、模板列表/详情、inspect、validate、render、400/404/413 的集成测试。

**完成标准**：高危文件读取路径不存在；服务模式的接口结构有自动化契约测试；全量测试、格式化和 Clippy 通过。

### 里程碑 S1：收口只读 Web 浏览（P1，对应路线图阶段 C）

1. 将服务页默认切至同源 server 模式，不硬编码 `127.0.0.1:8787`。
2. 页面加载真实模板和机床；模板详情由后端数据驱动。
3. 输出《演示模式差异清单》，至少覆盖未定义变量、默认值、数值过滤器和错误展示。
4. 更新 `ROADMAP.md` 与 `PROJECT_STATUS.md`：标明 C 阶段后端已实现项、仍未完成项及验收证据。
5. 限制单文件前端增长；超过 3000 行前把 API 适配和纯状态转换逻辑抽出为独立、可测试模块。

**完成标准**：浏览器能看到内置模板、目录模板和自定义机床；详情参数表与 `nctool inspect` 一致；不再使用 mock 作为服务模式数据源。

### 里程碑 S2：完成浏览器生成闭环（P2，对应路线图阶段 D）

1. 接入 `/api/validate`、`/api/render`，实现 300 ms 防抖和请求取消/序列保护。
2. 自动表单按规格渲染类型、必填、默认值、范围和单位；校验面板展示统一的 `error/warning/info`。
3. 接入生成选项、机床切换、复制与下载。
4. 建立逐字节回归：同一请求下 UI API 输出必须等于 CLI `nctool render` 输出。
5. `part generate` 保持独立范围，在当前交互闭环稳定后再排期。

**完成标准**：模板选择、填参、校验、预览、下载全链路可用，且与 Core/CLI 输出一致。

### 里程碑 S3：质量门、发布与领域验证（P1/P0）

1. 三平台 UI/CLI 关键路径验证，特别覆盖 Windows 路径与 CRLF/LF。
2. 把路径守卫、安全边界、依赖审计、请求上限和大模板/万行输出性能纳入质量门。
3. 恢复并修复 coverage job，避免长期 `continue-on-error` 掩盖退化。
4. 完成 `cargo install`、发布二进制、README/CHANGELOG 对齐与反馈机制。
5. 持续保持“模板开发工具”定位；在获得机床手册与工艺评审前，不得将内置模板或预设描述为可直接投产。

## 验证结果

- `cargo fmt --all -- --check`：通过。
- `cargo test --workspace`：通过，**288/288**（CLI 32 单元 + 39 集成、Core 84 单元 + 10 集成、Tpl 104 单元 + 18 集成 + 1 文档测试）。
- `cargo clippy --workspace --all-targets -- -D warnings`：失败，共 4 项。
  - 既有问题：`cli/src/cli.rs:16` 可派生的 `Default`；`cli/src/commands/templates.rs:60` 返回类型过于复杂。
  - 本次 UI 增量引入：`cli/src/server.rs:137` 冗余闭包；`cli/src/server.rs:583` 多余借用。

测试通过不能抵消本报告列出的安全和契约风险：现有自动化测试尚未覆盖路径穿越、真实前端服务模式或端到端 API 契约。

## 建议的执行顺序

1. 先处理 P0-1（路径读取）和 P0-3（DTO）。
2. 再完成 P0-2（真实前端接线），随后补真实 HTTP + 前端契约测试。
3. 通过 S1 验收后，决定是否投入 S2 的完整 Web 工作台；若资源有限，优先发布“CLI + 只读 Web 浏览”，推迟浏览器内生成与批量能力。
4. 每个里程碑合入前同步更新路线图、状态文档、测试计数和发布记录，避免文档再次落后代码。
